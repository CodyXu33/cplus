{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: build active file (exe only)",
            "command": "cl.exe",
            "args": [
                "/nologo",
                "/EHsc",
                "/utf-8",
                "/Fo${env:TEMP}\\${fileBasenameNoExtension}.obj",
                "/Fd${env:TEMP}\\",
                "/Fe${fileDirname}\\${fileBasenameNoExtension}.exe",
                "${file}",
                "/link",
                "/INCREMENTAL:NO"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$msCompile"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Build active file and keep only exe in workspace"
        },
        {
            "type": "cppbuild",
            "label": "C/C++: cl.exe 生成活动文件",
            "command": "cl.exe",
            "args": [
                "/Zi",
                "/EHsc",
                "/nologo",
                "/Fe${fileDirname}\\${fileBasenameNoExtension}.exe",
                "${file}"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$msCompile"
            ],
            "group": "build",
            "detail": "编译器: cl.exe"
        }
    ],
    "version": "2.0.0"
}
